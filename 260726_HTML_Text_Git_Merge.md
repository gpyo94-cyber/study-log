Deleted the same file on both local and GitHub -> divergent branches
push rejected -> git pull
setting pull ->git config --global pull.rebase false (=merge)
merge commit = a commit with 2 parents, created by joining two divergent histories
rule : work only on one side (local), then push. Don't edit both local and GitHub
pkill chrome - process kill (when chrome was stuck)

block elment : <p> , <h1~h6>
inline element : <em> <strong>
headings (h1~h6) have 6 levels
strong : bold + emphasis (강조)
em : italic + emphasis (강조)
closing pocess : most recently opened closes first
commnet : comments for who reading code

Bug of the day : missed closing bracket > on <meta charset="UTF-8"
Lesson : HTML doesn't throw errors. It silently breaks -> if the page looks wrong, check unclosed tags first