pwd (print working directory) : Shows the current directory location
ls (list) : list up
ls -a(list-all) : list all up
cd (change directory) : move directory to
mkdir (make directory) : make directory
touch : making file
rm (remove) : remove file
rm -r (remove-recursive) : remove folder
mv (move) : move (if you not moving locaion you can change name)
~ : tilde (symbol of home)
.. : upper folder 
. : current folder
sudo (superuser do) : superuser command
cat (concatenate) : Print file contents to terminal
-> There is 'print' for Python and it's for function
wget (web get) : get thing from site